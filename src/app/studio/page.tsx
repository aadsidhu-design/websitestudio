'use client';

import LovableBuilder from '@/components/lovable-builder';

export default function StudioPage() {
    return (
        <main className="min-h-screen bg-background">
            <LovableBuilder />
        </main>
    );
}
